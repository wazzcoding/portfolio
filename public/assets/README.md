# Stack logo assets

Put transparent PNG logos in this folder, then set the matching `asset` value in `src/heroConfig.js`.

Example:

```js
asset: '/assets/react.png'
```

Suggested names: `vscode.png`, `react.png`, `laravel.png`, `node.png`, `tailwind.png`, and `figma.png`.
